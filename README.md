# galactic-plotter 🌌

Before git clone, the requirement is Python 3.10.0 or later.

This Python package has the following functions:

This Python package will allow you to make the top-down density gas of any galaxy (supposedly) with the given position and velocity components from any sims. 
